define('main', ['angular', 'angular-resource'], function(angular) {    return angular.module('app', ['ngResource', 'bazalt-login'])    .config(['$routeProvider', '$locationProvider', '$httpProvider', 'bazaltLoginProvider',     function($routeProvider,   $locationProvider,   $httpProvider,   bazaltLoginProvider){        $locationProvider.html5Mode(true)                         .hashPrefix('!');        bazaltLoginProvider.baseUrl('/user');        $routeProvider        .when('/', {            template: 'Login',            controller: function() {            }        })        .otherwise({            redirectTo: '/'        });        $httpProvider.responseInterceptors.push('errorHttpInterceptor');    }]).run(function($route) {            console.info($route);    });});